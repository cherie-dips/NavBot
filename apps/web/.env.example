VITE_API_URL=http://localhost:3001
VITE_AUTH_URL=http://localhost:3000

# Absolute URL the generated embed snippet points at. Leave blank in development:
# the widget is served from this app's own origin as /chat-widget.iife.js, which is
# what the code falls back to. Set it in production only if the bundle is hosted
# somewhere other than the dashboard's origin (a CDN, say).
VITE_WIDGET_SCRIPT_URL=
